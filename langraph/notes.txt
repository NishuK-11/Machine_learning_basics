Tumne conda environment create kiya tha:

conda create -p D:\Ai-ml\langraph\venv python=3.10
pip install -r requirements.txt


Ye successfully create bhi ho gaya tha,
lekin jab tum activate kar rahe the:

conda activate D:\Ai-ml\langraph\venv


toh prompt change nahi ho raha tha —
matlab environment activate nahi hua ( (venv) appear nahi hua ).

🔹 Root Cause (Actual Problem)

Windows PowerShell by default block kar deta hai Conda ke activation scripts ko
due to its Execution Policy (security settings).

Is wajah se conda activate command execute toh hoti thi,
lekin Conda ka activation script run nahi kar paa raha tha,
isliye environment load nahi ho raha tha.

🧩 Solution Steps
✅ Step 1: Initialize Conda for PowerShell

Command:

conda init powershell


👉 Isse Conda apne activation scripts PowerShell ke startup file (Microsoft.PowerShell_profile.ps1) mein add kar deta hai,
taaki future mein conda activate properly work kare.



| Method             | Description                            |
| ------------------ | -------------------------------------- |
| `append(x)`        | Add element `x` to end                 |
| `extend(iterable)` | Add all elements from iterable         |
| `insert(i, x)`     | Insert `x` at index `i`                |
| `remove(x)`        | Remove first occurrence of `x`         |
| `pop([i])`         | Remove and return element at index `i` |
| `clear()`          | Remove all items                       |
| `index(x)`         | Return index of first occurrence       |
| `count(x)`         | Count occurrences of `x`               |
| `sort()`           | Sort the list in place                 |
| `reverse()`        | Reverse the list in place              |
| `copy()`           | Return a shallow copy                  |

squares = []
for i in range(1, 6):
    squares.append(i * i)

print(squares)
# Output: [1, 4, 9, 16, 25]


squares = [i * i for i in range(1, 6)]
print(squares)
# Output: [1, 4, 9, 16, 25]

matrix = [[col + row*3 for col in range(1, 4)] for row in range(3)]
print(matrix)
# Output:
# [[1, 2, 3],
#  [4, 5, 6],
#  [7, 8, 9]]

set 

| Feature          | Description                    |
| ---------------- | ------------------------------ |
| **Mutable**      | You can add or remove elements |
| **Unordered**    | No indexing or slicing         |
| **Unique items** | No duplicates allowed          |
| **Iterable**     | You can loop through it        |

Creating Sets
s1 = {1, 2, 3}
s2 = set([1, 2, 2, 3])   # duplicates removed → {1, 2, 3}
empty = set()            # ✅ correct way

🔹 Adding Elements
s = {1, 2, 3}
s.add(4)          # Add single element
s.update([5, 6])  # Add multiple elements

🔹 Removing Elements
s.remove(2)       # Removes 2 (Error if not found)
s.discard(10)     # Removes 10 (No error if not found)
s.pop()           # Removes a random element
s.clear()         # Removes all elements

🔹 Looping Through a Set
for item in s:
    print(item)

| Operation          | Symbol / Method                 | Example             | Output  |        |           |
| ------------------ | ------------------------------- | ------------------- | ------- | ------ | --------- |
| **Union**          | `                               | `/`.union()`        | `{1,2}  | {2,3}` | `{1,2,3}` |
| **Intersection**   | `&` / `.intersection()`         | `{1,2,3} & {2,3,4}` | `{2,3}` |        |           |
| **Difference**     | `-` / `.difference()`           | `{1,2,3} - {2}`     | `{1,3}` |        |           |
| **Symmetric Diff** | `^` / `.symmetric_difference()` | `{1,2,3} ^ {2,3,4}` | `{1,4}` |        |           |

Subset / Superset / Disjoint
A = {1, 2}
B = {1, 2, 3}
A.issubset(B)       # True
B.issuperset(A)     # True
A.isdisjoint({4,5}) # True

nums = {3, 1, 4, 5}
len(nums)      # 4
max(nums)      # 5
min(nums)      # 1
sum(nums)      # 13
sorted(nums)   # [1, 3, 4, 5]

A = {1, 2, 3}
B = A.copy()
A.add(4)
print(B)   # {1, 2, 3}

fs = frozenset([1, 2, 3])
# fs.add(4) ❌ → Error
print(fs)   # frozenset({1, 2, 3})

Converting Between Types
# Remove duplicates from list
nums = [1, 2, 2, 3]
unique = list(set(nums))
print(unique)  # [1, 2, 3]

Set Comprehension
squares = {x**2 for x in range(5)}
print(squares)  # {0, 1, 4, 9, 16}





Creating a Dictionary
# Using curly braces
my_dict = {"a": 1, "b": 2, "c": 3}

# Using dict() constructor
my_dict2 = dict(a=1, b=2, c=3)

# Empty dictionary
empty = {}

🔍 Accessing Values
student = {"name": "Nishu", "age": 21}

print(student["name"])   # Nishu
print(student.get("age"))  # 21
print(student.get("gender", "Not specified"))  # default value if key not found

✏️ Adding or Updating Items
student["gender"] = "Female"     # add new key
student["age"] = 22              # update existing key

❌ Removing Items
student.pop("age")          # remove a key by name
student.popitem()           # remove last inserted item
del student["name"]         # delete specific key
student.clear()             # remove all items

🔁 Looping Through a Dictionary
student = {"name": "Nishu", "age": 21, "course": "CS"}

# Loop through keys
for key in student:
    print(key)

# Loop through values
for value in student.values():
    print(value)

# Loop through key-value pairs
for key, value in student.items():
    print(key, ":", value)



🎯 Example: Counting Frequency
text = "banana"
freq = {}

for char in text:
    freq[char] = freq.get(char, 0) + 1

print(freq)  # {'b': 1, 'a': 3, 'n': 2}

🔐 Common Interview Trick
dict1 = {'a': 1, 'b': 2}
dict2 = dict1
dict2['a'] = 99
print(dict1)   # {'a': 99, 'b': 2}  (same reference!)

# To avoid this
dict2 = dict1.copy()
dict2['a'] = 99
print(dict1)   # {'a': 1, 'b': 2}



TUPLES:-

🛠️ Creating Tuples
t1 = (1, 2, 3)               # normal tuple
t2 = ("Nishu", 21, 5.6)      # mixed data
t3 = (1, 2, (3, 4))          # nested tuple
t4 = 1, 2, 3                 # packing (no brackets)
t5 = ()                      # empty tuple
t6 = (5,)                    # single-element tuple (comma required)

🔍 Accessing Elements
t = (10, 20, 30, 40)
print(t[0])     # 10
print(t[-1])    # 40
print(t[1:3])   # (20, 30)

❌ Updating / Deleting

Tuples are immutable, so you can’t directly modify or delete items.

✅ You can convert to a list → modify → convert back:

t = (1, 2, 3)
temp = list(t)
temp.append(4)
t = tuple(temp)
print(t)  # (1, 2, 3, 4)


Delete entire tuple:

del t

🔁 Iterating
t = ("apple", "banana", "cherry")
for i in t:
    print(i)

🧩 Tuple Methods
Method	Description	Example
tuple.count(x)	Count occurrences of x	t.count(2)
tuple.index(x)	Find index of x	t.index(3)

Example:

t = (1, 2, 3, 2, 2)
print(t.count(2))  # 3
print(t.index(3))  # 2

⚡ Tuple Operations
Operation	Example	Result
Concatenation	(1,2)+(3,4)	(1,2,3,4)
Repetition	(1,2)*2	(1,2,1,2)
Membership	3 in (1,2,3)	True
Length	len(t)	Number of elements
Slicing	t[1:3]	Subtuple
Iteration	for i in t:	Loop
📦 Tuple Packing

Packing means grouping multiple values into one tuple.

student = ("Nishu", 21, "CS")   # Packing
data = "Ravi", 22, "IT"         # Packing without parentheses

🔓 Tuple Unpacking

Unpacking means extracting elements of a tuple into variables.

student = ("Nishu", 21, "CS")
name, age, course = student
print(name)   # Nishu
print(age)    # 21


⚠️ Number of variables must match number of elements.

🔹 Partial Unpacking with *

You can capture remaining elements using the * operator:

data = (1, 2, 3, 4, 5)

a, *b = data
print(a)  # 1
print(b)  # [2, 3, 4, 5]

a, *b, c = data
print(a, b, c)  # 1 [2, 3, 4] 5

🧮 Useful Built-in Functions
Function	Description	Example
len(t)	Length	len((1,2,3)) → 3
max(t)	Maximum value	max((1,5,3)) → 5
min(t)	Minimum value	min((1,5,3)) → 1
sum(t)	Sum of all	sum((1,2,3)) → 6
sorted(t)	Returns sorted list	sorted((3,1,2)) → [1,2,3]
tuple(iterable)	Converts to tuple	tuple([1,2,3]) → (1,2,3)
🧱 Nested Tuples

Tuples can contain other tuples:

nested = ((1, 2), (3, 4), (5, 6))
print(nested[1][0])  # 3

⚖️ Tuple vs List
Feature	List	Tuple
Syntax	[ ]	( )
Mutable	✅ Yes	❌ No
Performance	Slower	Faster
Memory	More	Less
Hashable	❌ No	✅ Yes
Use Case	Dynamic data	Fixed data
💡 Use Cases

✅ When data should not change
✅ To store fixed-size records (e.g. coordinates, RGB values)
✅ As keys in dictionaries (since tuples are hashable)

🧭 Example: Tuple as Dictionary Key
location = {(20.59, 78.96): "India", (37.77, -122.41): "USA"}
print(location[(20.59, 78.96)])  # India

🧩 Example: Returning Multiple Values

Functions can return tuples:

def calc(a, b):
    return a+b, a*b

add, mul = calc(3, 4)
print(add, mul)  # 7 12


Lambda function :-
add = lambda a, b: a + b
print(add(5, 3))

def square_list(nums):
    return list(map(lambda x: x*x, nums))

print(square_list([1, 2, 3, 4]))

map function:-

numbers = [1, 2, 3, 4, 5]
result = map(str, numbers)
print(list(result))

a = [1, 2, 3]
b = [4, 5, 6]
result = map(lambda x, y: x + y, a, b)
print(list(result))
op/ [5, 7, 9]



If function is None, filter() removes all falsy values (0, False, None, '', [], etc.)

data = [0, 1, '', 'hello', [], [1, 2], None, True]
result = filter(None, data)
print(list(result))


✅ Output:

[1, 'hello', [1, 2], True]